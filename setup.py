from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A brief repo for building recommendation engine',
    author='singh vivek',
    license='MIT',
)
